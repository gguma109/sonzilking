# Cloudflare Pages Configuration
name = "sonjilwang"
compatibility_date = "2024-04-01"

[pages]
# Directory containing the static assets of your site
directory = "."

[[d1_databases]]
binding = "DB"
database_name = "sonjilwang-db"
# Note: For local development, wrangler uses a local SQLite mock.
# When deploying to production on Cloudflare Pages, you must bind
# a D1 database to the binding name 'DB' in the Pages project settings.
